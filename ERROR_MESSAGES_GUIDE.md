# 错误提示说明文档

## 概述

本项目使用统一的错误处理函数 `getFriendlyErrorMessage()` 将所有技术性错误转换为用户友好的中文提示。

## 常见错误说明

### 1. **"failed to fetch"（网络连接失败）**

**含义：** 无法连接到服务器或网络请求失败

**常见原因：**
- 网络连接断开
- 服务器暂时无法访问
- 防火墙或代理阻止了请求
- DNS 解析失败

**用户看到的提示：** 
> "网络连接失败，请检查网络后重试"

**解决方法：**
1. 检查网络连接是否正常
2. 刷新页面重试
3. 检查是否有防火墙或代理阻止
4. 等待片刻后重试

---

### 2. **"load failed"（资源加载失败）**

**含义：** 文件、图片或其他资源无法加载

**常见原因：**
- 文件不存在或已被删除
- 文件路径错误
- 服务器存储问题
- 网络连接中断

**用户看到的提示：** 
> "资源加载失败，请刷新页面后重试"

**解决方法：**
1. 刷新页面重试
2. 检查文件是否存在
3. 检查网络连接
4. 如问题持续，联系管理员

---

### 3. **"timeout"（请求超时）**

**含义：** 服务器响应时间过长，请求超时

**用户看到的提示：** 
> "请求超时，请稍后重试"

---

### 4. **"404 Not Found"（内容不存在）**

**含义：** 请求的内容不存在或已被删除

**用户看到的提示：** 
> "内容不存在或已被删除"

---

### 5. **"401 Unauthorized"（未授权）**

**含义：** 需要登录才能访问

**用户看到的提示：** 
> "请先登录"

---

### 6. **"403 Forbidden"（禁止访问）**

**含义：** 没有权限执行此操作

**用户看到的提示：** 
> "您没有权限执行此操作"

---

### 7. **"500 Internal Server Error"（服务器错误）**

**含义：** 服务器内部错误

**用户看到的提示：** 
> "服务器暂时无法处理，请稍后重试"

---

## 错误处理函数

所有错误都通过 `lib/utils.ts` 中的 `getFriendlyErrorMessage()` 函数统一处理：

```typescript
export function getFriendlyErrorMessage(error: any): string
```

### 功能特点：

1. **自动识别错误类型** - 根据错误消息自动匹配对应的友好提示
2. **中文优先** - 如果错误消息已经是中文，直接返回
3. **兜底机制** - 无法识别的错误返回通用提示 "操作失败，请重试"

### 使用示例：

```typescript
import { getFriendlyErrorMessage } from '@/lib/utils'

try {
  // 某个操作
} catch (error) {
  toast.error(getFriendlyErrorMessage(error) || '操作失败，请重试')
}
```

---

## 错误提示原则

1. **简单明了** - 避免使用技术术语
2. **提供解决方案** - 告诉用户如何解决
3. **友好提示** - 使用礼貌的语言
4. **中文优先** - 所有用户可见的错误提示都是中文

---

## 开发者说明

如果添加新的错误类型，请在 `lib/utils.ts` 的 `getFriendlyErrorMessage()` 函数中添加对应的错误识别和转换逻辑。

