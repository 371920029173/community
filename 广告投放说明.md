# 广告投放与运营说明（Cloudflare Pages + Next.js + Supabase）

本文档用于上线后续的广告投放与运营指导，默认你的网站已成功部署到 Cloudflare Pages，后端数据走 Supabase。

---

## 1. 当前线上环境速览

- 托管：Cloudflare Pages（Edge Runtime）
- 主要变量：
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - `SUPABASE_SERVICE_ROLE_KEY`
- 诊断接口：
  - `GET /api/verify-keys`（检查变量是否被正确读取，仅校验格式）
  - `GET /api/profile/me`（登录后验证 Edge 到数据库的读权限）

---

## 2. 广告投放的三种常见形态

> 建议从简单到复杂，逐步上线，避免一次性影响全站体验。

1) 图片/富文本 Banner（最简单）
   - 静态/动态图，点击跳转到外链或站内落地页。
   - 优点：无第三方脚本、可控强、合规风险低。
   - 缺点：无自动竞价；需自行维护投放与效果。

2) AdSense/字节穿山甲/百度联盟等脚本广告（变现能力强）
   - 通过 `<script>` 注入第三方 SDK，在指定挂位展示广告。
   - 优点：自动竞价、有填充率与收益报表。
   - 缺点：需要做好 CSP、安全、性能与合规（Cookie/隐私）处理。

3) 原生/组件化广告（深度融合 UI）
   - 在列表卡片中穿插「推荐」项，样式贴近站内内容。
   - 优点：点击率高、体验统一。
   - 缺点：需要清晰标注「广告/推广」，避免误导。

---

## 3. 推荐的页面挂位（初版）

- 顶部导航右侧（小图标旁）——小尺寸图标位/徽标位
- 首页 Hero 下方——横幅 Banner（常见 728x90 / 970x90 / 全宽自适应）
- 列表页每 N 条内容后插入一条「推荐」卡片（N = 6~10）
- 文章/详情顶部与底部各一条 Banner
- 移动端：列表中部 300x250、底部 320x50/100

命名建议：`ad-navbar-badge`、`ad-home-top-banner`、`ad-list-infeed-1`、`ad-detail-bottom` 等，便于统计与 A/B。

---

## 4. 组件化广告实现（示例）

### 4.1 自有 Banner（零脚本，推荐作为起步）

```tsx
// components/ad/Banner.tsx
import Image from 'next/image'

type BannerProps = {
  imgUrl: string
  linkUrl: string
  alt?: string
  width?: number
  height?: number
  className?: string
}

export default function Banner({ imgUrl, linkUrl, alt = 'sponsor', width = 1200, height = 200, className = '' }: BannerProps) {
  return (
    <a href={linkUrl} target="_blank" rel="nofollow noopener" className={`block ${className}`}> 
      <Image src={imgUrl} alt={alt} width={width} height={height} priority sizes="100vw" className="w-full h-auto rounded" />
    </a>
  )
}
```

挂载示例：

```tsx
// app/page.tsx（首页）
import dynamic from 'next/dynamic'
const AdBanner = dynamic(() => import('@/components/ad/Banner'), { ssr: false })

export default function Home() {
  return (
    <main>
      {/* Hero ... */}
      <section className="container mx-auto px-4 mt-6">
        <AdBanner
          imgUrl="/ads/launch-2025.png"  // 放在 public/ads
          linkUrl="https://your-sponsor.example.com/?utm_source=site&utm_medium=banner_home_top"
          width={1200}
          height={200}
        />
      </section>
      {/* 其他内容 ... */}
    </main>
  )
}
```

### 4.2 Google AdSense（脚本型示例）

1) 申请并获取 **`ca-pub-xxxxxxxxxxxxxxxx`** 与 **ad-slot**。

2) 配置 CSP（可选但建议）：在 `Cloudflare Pages → Security → Content Security Policy` 或自定义 Headers 中增加允许域：

```
script-src 'self' https://pagead2.googlesyndication.com https://www.googletagservices.com https://googleads.g.doubleclick.net;
img-src 'self' https://pagead2.googlesyndication.com https://tpc.googlesyndication.com https://googleads.g.doubleclick.net data:;
frame-src https://googleads.g.doubleclick.net https://tpc.googlesyndication.com;
```

3) 组件封装：

```tsx
// components/ad/AdSense.tsx
import { useEffect } from 'react'

export default function AdSense({ slot, format = 'auto', style = { display: 'block' } }: { slot: string; format?: string; style?: any }) {
  useEffect(() => {
    try { (window as any).adsbygoogle = (window as any).adsbygoogle || []; (window as any).adsbygoogle.push({}); } catch {}
  }, [])

  return (
    <>
      <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-xxxxxxxxxxxxxxxx" crossOrigin="anonymous" />
      <ins className="adsbygoogle" style={style} data-ad-client="ca-pub-xxxxxxxxxxxxxxxx" data-ad-slot={slot} data-ad-format={format} />
    </>
  )
}
```

挂载：

```tsx
// 某页面
import dynamic from 'next/dynamic'
const AdSense = dynamic(() => import('@/components/ad/AdSense'), { ssr: false })

<AdSense slot="1234567890" />
```

> 注意：首次投放可能需要 10-30 分钟才会开始填充；请在 AdSense 后台开启该域名，并遵守其政策（成人/下载类限制等）。

---

## 5. 统计与追踪（Supabase）

### 5.1 建表

```sql
create table if not exists ad_impressions (
  id bigint generated by default as identity primary key,
  ad_key text not null,           -- 挂位唯一键，如 ad-home-top-banner
  user_id uuid,                   -- 可空
  ip inet,                        -- 可按需
  ua text,
  created_at timestamptz default now()
);

create table if not exists ad_clicks (
  id bigint generated by default as identity primary key,
  ad_key text not null,
  user_id uuid,
  ip inet,
  ua text,
  created_at timestamptz default now()
);
```

### 5.2 记录曝光/点击（边缘 API）

```ts
// app/api/ads/track/route.ts
import { NextRequest, NextResponse } from 'next/server'
export const runtime = 'edge'

export async function POST(req: NextRequest) {
  try {
    const { adKey, type } = await req.json() as { adKey: string; type: 'impression'|'click' }
    if (!adKey || !type) return NextResponse.json({ ok: false, error: 'bad params' }, { status: 400 })

    const url = process.env.NEXT_PUBLIC_SUPABASE_URL!
    const key = process.env.SUPABASE_SERVICE_ROLE_KEY!
    const table = type === 'click' ? 'ad_clicks' : 'ad_impressions'

    const res = await fetch(`${url}/rest/v1/${table}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', apikey: key, Authorization: `Bearer ${key}` },
      body: JSON.stringify({ ad_key: adKey })
    })

    return NextResponse.json({ ok: res.ok })
  } catch (e: any) {
    return NextResponse.json({ ok: false, error: String(e?.message || e) }, { status: 500 })
  }
}
```

前端曝光/点击上报：

```ts
// utils/ads.ts
export async function trackAd(adKey: string, type: 'impression'|'click') {
  try {
    await fetch('/api/ads/track', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ adKey, type }) })
  } catch {}
}
```

在 Banner 组件中：

```tsx
// components/ad/Banner.tsx 中
import { useEffect } from 'react'
import { trackAd } from '@/utils/ads'

export default function Banner(...) {
  useEffect(() => { trackAd('ad-home-top-banner', 'impression') }, [])
  return (
    <a ... onClick={() => trackAd('ad-home-top-banner', 'click')}>
      ...
    </a>
  )
}
```

---

## 6. A/B 与灰度

### 6.1 通过环境变量做开关
在 `wrangler.toml` `[vars]` 里加：

```
FEATURE_ADS = "true"          # 总开关
ADS_VARIANT = "A"              # A/B 版本（A/B/C）
```

前端读取：

```ts
export const FEATURE_ADS = process.env.NEXT_PUBLIC_FEATURE_ADS === 'true'
export const ADS_VARIANT = process.env.NEXT_PUBLIC_ADS_VARIANT || 'A'
```

### 6.2 通过 Supabase 表
- 建表 `ad_flags`：按用户、路径、设备、登录态做更细粒度控制。

---

## 7. 性能与体验

- 图片广告优先使用 `next/image`，并合理设置宽高、开启压缩。
- 懒加载（在视口内再加载）；对脚本广告使用动态导入 + `ssr:false`。
- 限制页面同时挂载的脚本实例数量，核心位优先，次要位按曝光率。
- 移动端注意高度占用与关闭按钮；避免遮挡交互区域。

---

## 8. 合规与安全

- GDPR/CCPA/COPPA：若面向欧盟/加州/未成年人用户，需弹窗征求同意或做年龄屏蔽。
- 隐私政策与用户条款页面需列出使用的第三方（Google、统计、CDN 等）。
- 对第三方脚本设置 `rel="nofollow noopener"`、`crossOrigin`、`sandbox`（iframe）等。
- CSP 与子资源完整性（SRI）可降低脚本被篡改的风险。

---

## 9. 投放流程（落地可用）

1) 与广告主对齐目标与计费（CPM/CPC/固定包断/混合）。
2) 确认挂位与素材规格（PC/移动、像素/比例、文件大小）。
3) 在 `public/ads/` 放素材；或接入第三方脚本。
4) 通过组件方式在目标页面挂载，并在 `utils/ads.ts` 上报曝光/点击。
5) 后台（Supabase SQL 或仪表盘）查看 `ad_impressions/ad_clicks` 统计。
6) 每周/每月复盘转化效果，调整挂位、创意与出价。

---

## 10. 常见问题

**Q1: AdSense 不展示？**  
A1: 首次审核需要时间；确保域名已在 AdSense 后台授权；CSP 放行；广告单元 `ad-slot` 正确，且组件 `ssr:false`。

**Q2: 点击记录不到？**  
A2: 检查 `/api/ads/track` 请求是否 200；控制台是否有跨域/权限报错；确认 service role key 正确、RLS 对 REST 插入开放（或使用服务端 key）。

**Q3: 影响页面速度？**  
A3: 动态导入脚本广告；降低同时展示数量；把次要位延后插入；结合 Cloudflare 缓存与图片压缩。

---

## 11. 下一步计划（建议）

- 后台投放面板（增/改/删广告位、素材上架、投放计划、定向规则）
- 可视化报表（曝光、点击、CTR、转化漏斗、eCPM）
- A/B 自动化（多版本素材自动轮换、最优胜出）
- 与支付/订单打通（按点击/曝光自动对账）

---

如需我代为落地上述任意一步（挂位、AdSense、统计、A/B、报表），直接在工单里说“执行第 X 步”，我会按你的站点结构直接提交代码与配置。


